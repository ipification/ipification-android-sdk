package com.ipification.mobile.sdk.im

/** UI colors used by the SDK-hosted IM verification screen. */
data class IMTheme(
    /** Background color applied to the verification screen. */
    var backgroundColor: Int?,

    /** Text color applied to the toolbar title. */
    var toolbarTextColor: Int?,

    /** Background color applied to the toolbar. */
    var toolbarColor: Int?
)
